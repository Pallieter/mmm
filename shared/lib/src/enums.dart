enum Context {
  longFocusWorkInteractive,
  longFocusWorkOffline,
  longFocusWorkOnline,
  shortTaskOffline,
  shortTaskOnline,
  shortTaskInteractive,
  locationBased,
}

enum Priority {
  now,
  today,
  soon,
  later,
  specificDate,
}

enum AccessLevel {
  private,
  sharedWithWife,
  sharedWithAssistants,
  public,
}

enum UserRole {
  owner,
  wife,
  assistant,
}
